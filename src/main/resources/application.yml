spring:
  output:
     ansi:
         enabled: always

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  messages:
    basename : messages
server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG



